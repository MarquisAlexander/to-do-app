import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	emptyListTitle: {
		color: "#808080",
		fontWeight: "bold",
		paddingTop: 16,
	},
	emptyListSubTitle: {
		color: "#808080",
	},
});
